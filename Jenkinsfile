pipeline {
    agent any
    stages {
        stage('Build Images') {
            steps {
                echo 'Initiating Building Docker images'
                sh 'ls -la'
            }
        }
    }
}